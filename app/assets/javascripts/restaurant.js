$(document).ready(function(){
  // record the first jumbotron
  // render the first restaurant (sub 0?)
  // add information to the data being presented
  // remove the first restaurant
  // present the second one
})
