class UsersController < ApplicationController

  def show
    # needs to have options for search then the action that send the search
    # maybe a checkbox then grab that action in params the make the location call
    # then pass the action and location to the search
    # load the search data
    # how will you get from one data set to the other?
    # should all data be saved in a cache collection so there is no continual loading?
  end
end
